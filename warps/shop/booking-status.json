{
  "protocol": "warp:3.0.0",
  "name": "Shop: Booking Status",
  "title": {
    "en": "Booking status",
    "de": "Buchungsstatus"
  },
  "description": {
    "en": "Check whether online booking is enabled for this shop. Queries the on-chain bookable flag to determine if customers can book appointments.",
    "de": "Prüfe, ob Online-Buchung für diesen Shop aktiviert ist. Fragt den On-Chain-Buchungs-Flag ab, um festzustellen, ob Kunden Termine buchen können."
  },
  "bot": "Returns the shop's bookable flag. Use this before loading services or attempting to book an appointment.",
  "chain": "multiversx",
  "vars": {
    "SC_ADDRESS": "INJECT:SHOP_SC_ADDRESS"
  },
  "actions": [
    {
      "type": "query",
      "label": {
        "en": "Check booking status",
        "de": "Buchungsstatus prüfen"
      },
      "address": "{{SC_ADDRESS}}",
      "func": "getShopSetting",
      "args": [
        "string:{{SHOP_ID}}",
        "string:bookable"
      ],
      "inputs": [
        {
          "name": "SHOP_ID",
          "type": "string",
          "position": "arg:1",
          "source": "hidden",
          "default": "{{JOAI_WARP_BRAND}}",
          "required": true
        }
      ],
      "auto": true,
      "abi": "INJECT:contract/output/shop.abi.json"
    }
  ],
  "output": {
    "BOOKING_ENABLED": "out.1"
  }
}
