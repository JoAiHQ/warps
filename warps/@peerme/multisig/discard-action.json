{
  "protocol": "warp:3.0.0",
  "chain": "multiversx",
  "name": "PeerMe MultiSig: Discard Action",
  "title": "Discard a proposal",
  "description": "Discard a MultiSig proposal that is no longer needed. All signatures must be removed first using unsign. This frees up storage and prevents the proposal from being executed.",
  "vars": {
    "MULTISIG_ADDRESS": "query:multisig"
  },
  "actions": [
    {
      "type": "contract",
      "label": "Discard proposal",
      "address": "{{MULTISIG_ADDRESS}}",
      "func": "discardAction",
      "args": [],
      "gasLimit": 10000000,
      "abi": "INJECT:peerme/multisig.abi.json",
      "inputs": [
        {
          "name": "Action ID",
          "as": "ACTION_ID",
          "description": "The ID of the proposal to discard. All signatures must be removed first.",
          "type": "number",
          "position": "arg:1",
          "source": "field",
          "required": true
        }
      ]
    }
  ],
  "output": {
    "SUCCESS": "out"
  },
  "messages": {
    "success": "Proposal {{ACTION_ID}} has been discarded successfully.",
    "card": "Discarded Proposal #{{ACTION_ID}}"
  }
}
