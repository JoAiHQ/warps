{
  "protocol": "warp:3.0.0",
  "name": "Mail: Draft",
  "title": {
    "en": "Draft email",
    "de": "E-Mail verfassen"
  },
  "description": {
    "en": "Draft a personalized email to a CRM contact. Logs the email as a contact activity and returns a one-click mailto link that opens your default mail client with everything prefilled — review and send.",
    "de": "Eine personalisierte E-Mail an einen CRM-Kontakt verfassen. Protokolliert die E-Mail als Kontakt-Aktivität und liefert einen Ein-Klick-Mailto-Link, der deinen Standard-Mail-Client mit allem vorausgefüllt öffnet — prüfen und senden."
  },
  "bot": "Use this when the user wants to send an email to a CRM contact. The agent composes the full subject and body, then this warp logs it as a contact activity (type=email) and returns a mailto link. The user clicks the link in the success message to open their mail client with subject and body prefilled. The activity is searchable in the contact history and feeds follow-up signals. Do not use for broadcasts (use outreach-broadcast instead) or for sending without CRM logging.",
  "actions": [
    {
      "type": "collect",
      "auto": true,
      "label": {
        "en": "Log email & generate mailto link",
        "de": "E-Mail protokollieren & Mailto-Link generieren"
      },
      "destination": {
        "url": "INJECT:API_BASE/v1/contacts/{{contactId}}/activities",
        "method": "POST",
        "headers": {
          "X-Agent-Auth-Key": "{{JOAI_AGENT_AUTH_KEY}}",
          "Content-Type": "application/json"
        }
      },
      "inputs": [
        {
          "name": "Contact",
          "as": "contactId",
          "description": {
            "en": "CRM contact ID",
            "de": "CRM-Kontakt-ID"
          },
          "bot": "The ID of the CRM contact to email. Must exist — create the contact first if needed.",
          "type": "string",
          "source": "field",
          "required": true
        },
        {
          "name": "Subject",
          "as": "subject",
          "description": {
            "en": "Email subject line",
            "de": "E-Mail-Betreff"
          },
          "bot": "Concise, specific subject line. Avoid salesy language. For cold outreach, reference the recipient's work or a shared context.",
          "type": "string",
          "source": "field",
          "required": true,
          "max": 200
        },
        {
          "name": "Body",
          "as": "body",
          "description": {
            "en": "Full email body",
            "de": "Vollständiger E-Mail-Inhalt"
          },
          "bot": "The complete email body. Plain text with line breaks. Write it as the final message — the user will review but the default should be send-ready.",
          "type": "string",
          "source": "field",
          "required": true,
          "max": 5000
        },
        {
          "name": "Recipient Email",
          "as": "recipientEmail",
          "description": {
            "en": "Recipient email address",
            "de": "E-Mail-Adresse des Empfängers"
          },
          "bot": "The email address to send to. Usually already on the contact record — provide explicitly here to ensure the mailto link resolves correctly.",
          "type": "string",
          "source": "field",
          "required": true
        },
        {
          "name": "Recipient Name",
          "as": "recipientName",
          "description": {
            "en": "Recipient display name",
            "de": "Anzeigename des Empfängers"
          },
          "bot": "Optional display name for the recipient.",
          "type": "string",
          "source": "field",
          "required": false
        },
        {
          "name": "PAYLOAD",
          "as": "PAYLOAD",
          "type": "string",
          "source": "hidden",
          "modifier": "transform:(inputs) => JSON.stringify({ type: 'email', description: inputs.body, meta: { recipientEmail: inputs.recipientEmail, recipientName: inputs.recipientName || null, subject: inputs.subject } })"
        }
      ]
    }
  ],
  "output": {
    "MAIL_ACTIVITY_ID": "out.data.id",
    "MAIL_MAILTO_URL": "transform:`mailto:${inputs.recipientEmail}?subject=${encodeURIComponent(inputs.subject)}&body=${encodeURIComponent(inputs.body)}`"
  },
  "messages": {
    "success": {
      "en": "Draft ready. [Open in mail client →]({{MAIL_MAILTO_URL}})",
      "de": "Entwurf bereit. [Im Mail-Client öffnen →]({{MAIL_MAILTO_URL}})"
    }
  }
}
